# Django settings (`web/webapp/settings.py`) and SQLAlchemy (`src/land_monitor/db.py`)
# both read environment variables from the repository root `.env`.
LAND_DB_HOST=localhost
LAND_DB_PORT=5432
LAND_DB_NAME=land_monitor
LAND_DB_USER=land_user
LAND_DB_PASSWORD=
