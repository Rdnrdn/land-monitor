# Django web читает корневой `.env` из репозитория.
# Ниже перечислены только переменные, реально нужные для подключения к PostgreSQL.
LAND_DB_HOST=localhost
LAND_DB_PORT=5432
LAND_DB_NAME=land_monitor
LAND_DB_USER=land_user
LAND_DB_PASSWORD=
